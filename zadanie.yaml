Это упражнение должно быть выполнено локально используя terraform и docker.

 Используя terraform и любой docker провайдер , создаст следующий кластер:

Запустить x (x это количество) web-servers  ,серверы с единственной статичной странницей с сообщение: Hello from web-server (1..X).

Запустить load-balancer перед веб-серверами который  осуществит балансировку по round-robin.

На обоих веб серверах. и на load-balancer , добавить  хелф эндпоинт возвращающий имя компонента (web-server-(1..X) / load-balancer).

Создать поддержку для настроек различныx версиий для веб-сервера и сетевого балансировщика

Написать shell script для установки (используя git pull из github) cтарта стопа и статуса кластера